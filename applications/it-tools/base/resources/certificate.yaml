apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: it-tools-tls
  namespace: istio-system
spec:
  secretName: it-tools-tls
  issuerRef:
    name: cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - it-tools.svc.home.arpa
