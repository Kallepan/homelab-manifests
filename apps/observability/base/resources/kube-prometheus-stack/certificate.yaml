apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: prometheus-tls
  namespace: istio-system
spec:
  secretName: prometheus-tls
  issuerRef:
    name: cluster-issuer
    kind: ClusterIssuer
  dnsNames:
    - prometheus.svc.home.arpa
    - alerts.svc.home.arpa
